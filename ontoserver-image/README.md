# Ontoserver (preloaded)

A Docker build of Ontoserver that pre-loads a known set
of code systems and value sets. The resulting Docker image is
set up to be run 'read-only' as a public ontology service.
